$(document).ready(function(){
	// $(".registry-form").parent().parent().find("button").hide();
  $('.galleries-container').slick({
    infinite: true,
    slidesToShow: 3,
    slidesToScroll: 1,
    dots: true,
    // centerMode: true
    // accessibility: false,
    // focusOnSelect: true

  });
});
